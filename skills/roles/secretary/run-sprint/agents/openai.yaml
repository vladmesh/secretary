interface:
  display_name: "Run Sprint"
  short_description: "Autonomously take the active sprint to its goal"
  default_prompt: "Use $run-sprint to autonomously take the active sprint to its goal."
policy:
  allow_implicit_invocation: false
