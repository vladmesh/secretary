{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "adapter.schema.json",
  "title": "Project adapter",
  "description": "How the pipeline sets up, smoke-checks and validates a project (secretary-instance/adapters/<id>.yaml).",
  "type": "object",
  "additionalProperties": false,
  "required": ["setup", "smoke", "validation", "artifact_policy"],
  "properties": {
    "setup": {
      "type": "object",
      "additionalProperties": false,
      "required": ["commands"],
      "properties": {
        "commands": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1},
          "description": "Setup commands run on a clean worktree."
        }
      }
    },
    "smoke": {
      "type": "object",
      "additionalProperties": false,
      "required": ["command"],
      "properties": {
        "command": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "validation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ci"],
      "properties": {
        "ci": {
          "type": "string",
          "enum": ["github", "local", "none"],
          "description": "Where validation runs. 'local' needs a command, 'none' must be justified by 'missing'."
        },
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "Local validation command, required when ci is 'local'."
        },
        "missing": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1},
          "description": "What the gate could not cover, e.g. ['tests']. Required when ci is 'none'."
        },
        "required_checks": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1},
          "description": "GitHub check names the gate judges by, e.g. ['test']. Matches an Actions check-run name or a legacy commit status context. Only meaningful when ci is 'github'; without it the gate judges by every check on the sha."
        }
      },
      "allOf": [
        {
          "if": {"required": ["ci"], "properties": {"ci": {"const": "local"}}},
          "then": {"required": ["command"]}
        },
        {
          "if": {"required": ["ci"], "properties": {"ci": {"const": "none"}}},
          "then": {"required": ["missing"]}
        }
      ]
    },
    "artifact_policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["write_project_files"],
      "properties": {
        "write_project_files": {
          "type": "boolean",
          "description": "Whether the pipeline may write files into the project repo."
        }
      }
    },
    "broad_check": {
      "type": "object",
      "additionalProperties": false,
      "required": ["import_package"],
      "description": "The project's own broad suite: which module `check broad --module` runs, which package that run must import, and optionally which interpreter runs it. `additionalProperties: false` is why an unknown key here is refused outright, so this block has to describe the whole contract. Declaring it is mandatory for a project that gets cards: an adapter without it is refused as `broad_check_not_declared` and its cards block at preflight (issue:81a0a1e5c15225fa360e). It is not `required` here, because a missing contract is a fact about the project rather than a malformed adapter, and refusing it as `adapter_invalid` would hide which of the two is wrong.",
      "properties": {
        "interpreter": {
          "type": "string",
          "minLength": 1,
          "description": "Optional interpreter for module broad checks. A relative path is resolved from the candidate workspace, for example .venv/bin/python. Omitted, the check runs on the interpreter that runs the wrapper: since the check subprocess prepends the candidate's own import roots to sys.path, a shared installation interpreter still imports the candidate rather than production, and a project whose worktrees have no venv of their own needs no nonexistent workspace/.venv (issue:8b39e60e4df361c6138e)."
        },
        "import_package": {
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*(\\.[A-Za-z_][A-Za-z0-9_]*)*$",
          "description": "Package the check process must import and prove came from the candidate workspace."
        },
        "module": {
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*(\\.[A-Za-z_][A-Za-z0-9_]*)*$",
          "description": "The module a `python -m` runs: this project's broad suite. Without it nobody but the project itself could say which suite a broad check means, and the worker prompt had to print a placeholder. Not required by the schema, so an adapter written before this key existed is not suddenly malformed, and PR #331 stopped the contract layer refusing such a block too: omitting it means only that the caller has to name the suite itself, which `check broad`/`check show` with no `--module` reports as `no_broad_check_module`."
        },
        "args": {
          "type": "array",
          "items": {"type": "string"},
          "description": "The exact argument vector passed after the module. Kept as a list, never a rendered string, because `-k a b` and `-k 'a b'` render identically while running different checks."
        }
      }
    }
  }
}
