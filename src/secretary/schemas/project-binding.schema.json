{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "project-binding.schema.json",
  "title": "Project binding",
  "description": "One connected project (secretary-instance/projects/<id>.yaml).",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "repo", "enabled", "adapter", "default_branch"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "description": "Stable project id, kebab-case."
    },
    "repo": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute checkout path on the managed host."
    },
    "remote": {
      "type": "string",
      "minLength": 1,
      "description": "Git remote used to recreate a missing checkout."
    },
    "orca_binding": {
      "type": "string",
      "minLength": 1,
      "description": "Explicit durable Orca registration name. It is never derived from id or repo and may be present while task routing remains disabled."
    },
    "curator_roots": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^/",
        "minLength": 2
      },
      "uniqueItems": true,
      "description": "Optional absolute historical cwd roots used only to route curator input after an ad-hoc checkout has been removed. They grant no execution authority."
    },
    "enabled": {
      "type": "boolean",
      "description": "Only true after a green onboarding gate."
    },
    "adapter": {
      "type": "string",
      "minLength": 1,
      "description": "Adapter id, resolved under adapters/."
    },
    "default_branch": {
      "type": "string",
      "minLength": 1
    },
    "integration_bases": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true,
      "description": "Long-lived branches besides default_branch that this project integrates into and whose CI its workflows trigger for. A card's workspace.base_branch may name one of these or the default branch, and nothing else; a per-card branch is never an integration target."
    },
    "plane": {
      "type": "string",
      "enum": ["orchestrator", "project", "memory", "ops"],
      "description": "Routing plane for this project's cards."
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "code_concurrency": {
          "type": "integer",
          "minimum": 0
        }
      }
    }
  }
}
