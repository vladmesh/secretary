"""Alembic's script directory for the board store (``docs/BOARD_STORE.md`` §7.4).

It is a package so that ``env.py``, the template and the revisions ship with the installed
product rather than only existing in a checkout; Alembic itself loads them by path.
"""
