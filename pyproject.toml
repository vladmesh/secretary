[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "secretary"
version = "0.1.0"
description = "Personal secretary appliance orchestrator"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Private" }

[project.scripts]
secretary = "secretary.cli:main"
