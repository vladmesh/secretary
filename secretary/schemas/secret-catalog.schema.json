{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "secret-catalog.schema.json",
  "title": "Secret catalog",
  "description": "Open metadata for the encrypted secret store in secrets/catalog.yaml. Values never appear here; every field is safe to read without the installation key.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "secrets"],
  "properties": {
    "version": {
      "description": "Catalog format version.",
      "const": 1
    },
    "secrets": {
      "type": "array",
      "description": "One entry per stored secret, sorted by id.",
      "items": {"$ref": "#/$defs/entry"}
    }
  },
  "$defs": {
    "entry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "scope", "purpose", "created_at"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 128,
          "pattern": "^[a-z0-9][a-z0-9._-]*$",
          "description": "Stable identifier, also the name of the envelope file under secrets/values/."
        },
        "scope": {
          "type": "string",
          "pattern": "^(installation|project:[a-z0-9][a-z0-9._-]*)$",
          "description": "Who the secret belongs to: the installation itself, or one project binding."
        },
        "purpose": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500,
          "description": "What the secret is for, in plain words. Read by a recovery that cannot open the value."
        },
        "environment": {
          "type": "string",
          "minLength": 1,
          "maxLength": 64,
          "description": "Environment variable name this value materializes into, when it materializes at all."
        },
        "created_at": {
          "type": "string",
          "minLength": 20,
          "description": "UTC timestamp of the first write, RFC 3339."
        }
      }
    }
  }
}
