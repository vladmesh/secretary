"""Bundled JSON Schema documents for instance config, project bindings,
adapters and the data manifest.

The schema files are the contract between the ``secretary`` product and a
``secretary-instance`` config repo. They are loaded at runtime by
``secretary.config``.
"""
