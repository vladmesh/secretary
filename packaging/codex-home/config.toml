model = "gpt-5.6-sol"
model_reasoning_effort = "medium"

# The installation materializer replaces this placeholder with the absolute
# PO-bridge executable and data-dir environment for the selected installation.
[mcp_servers.po_memory]
enabled = false
